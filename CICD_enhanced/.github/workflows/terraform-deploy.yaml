# name: CD - Terraform Deploy

# on:
#   push:
#     branches:
#       - main
#     paths:
#       - 'environments/**'
#       - 'modules/**'
#   workflow_dispatch:
#     inputs:
#       environment:
#         description: 'Target environment'
#         required: true
#         default: 'dev'
#         type: choice
#         options:
#           - dev
#           - prod
#       action:
#         description: 'Terraform action'
#         required: true
#         default: 'apply'
#         type: choice
#         options:
#           - apply
#           - destroy
#       destroy_confirm:
#         description: 'Type DESTROY to confirm destroy action'
#         required: false
#         default: ''

# env:
#   AWS_REGION: ap-southeast-1
#   TERRAFORM_VERSION: 1.9.5
#   BUCKET_TF_STATE: ${{ secrets.BUCKET_TF_STATE }}
#   SOFT_FAIL: false

# # Prevent concurrent deployments to the same environment
# concurrency:
#   group: deploy-${{ github.event.inputs.environment || 'dev' }}
#   cancel-in-progress: false

# jobs:
#   # =======================================================
#   # JOB 1: Validate before any deployment
#   # =======================================================
#   validate:
#     name: Validate Terraform
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v2
#         with:
#           terraform_version: ${{ env.TERRAFORM_VERSION }}

#       - name: Resolve target environment
#         id: env
#         run: |
#           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
#             echo "target=${{ github.event.inputs.environment }}" >> $GITHUB_OUTPUT
#             echo "action=${{ github.event.inputs.action }}"      >> $GITHUB_OUTPUT
#           else
#             echo "target=dev"   >> $GITHUB_OUTPUT
#             echo "action=apply" >> $GITHUB_OUTPUT
#           fi

#       - name: Terraform format check
#         run: terraform fmt -check -recursive

#       - name: Terraform init
#         working-directory: ./environments/${{ steps.env.outputs.target }}
#         run: terraform init -backend=false

#       - name: Terraform validate
#         working-directory: ./environments/${{ steps.env.outputs.target }}
#         run: terraform validate

#   # =======================================================
#   # JOB 2: Security gate - must pass before deploy
#   # =======================================================
#   security-gate:
#     name: Security Gate
#     runs-on: ubuntu-latest
#     needs: validate
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.11'

#       - name: Setup OPA
#         run: |
#           mkdir -p /tmp/opa
#           curl -L -o /tmp/opa/opa https://openpolicyagent.org/downloads/latest/opa_linux_amd64
#           chmod +x /tmp/opa/opa
#           /tmp/opa/opa version

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v2
#         with:
#           terraform_version: ${{ env.TERRAFORM_VERSION }}

#       - name: Install Checkov
#         run: pip install checkov --quiet

#       - name: Resolve target environment
#         id: env
#         run: |
#           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
#             echo "target=${{ github.event.inputs.environment }}" >> $GITHUB_OUTPUT
#           else
#             echo "target=dev" >> $GITHUB_OUTPUT
#           fi

#       - name: Run Checkov pre-deploy scan
#         run: |
#           mkdir -p reports
#           checkov \
#             --directory environments/${{ steps.env.outputs.target }} \
#             --framework terraform \
#             --check CKV_AWS_* \
#             --output json \
#             --output-file-path reports \
#             --soft-fail \
#             --download-external-modules false \
#             --config-file .checkov.yaml \
#             2>&1 | tail -30 || true

#       - name: Generate Terraform plan JSON for OPA
#         working-directory: ./environments/${{ steps.env.outputs.target }}
#         run: |
#           terraform init -backend-config="bucket=${{ env.BUCKET_TF_STATE }}"
#           terraform plan -out=tfplan
#           terraform show -json tfplan > ${{ runner.temp }}/tf-plan.json

#       - name: Run OPA policy checks
#         id: opa
#         run: |
#           /tmp/opa/opa eval \
#             -d policies/ \
#             -I \
#             "data.terraform.deny" \
#             < ${{ runner.temp }}/tf-plan.json \
#             > ${{ runner.temp }}/opa-deny.json 2>&1 || true

#       - name: Check OPA results
#         run: |
#           VIOLATIONS=$(cat ${{ runner.temp }}/opa-deny.json | jq '.result[0].expressions[0].value | length' 2>/dev/null || echo 0)
#           echo "OPA policy violations: $VIOLATIONS"
#           if [ "$VIOLATIONS" -gt 0 ]; then
#             echo "Policy violations found:"
#             cat ${{ runner.temp }}/opa-deny.json | jq '.result[0].expressions[0].value[]' 2>/dev/null || true
#             exit 1
#           fi

#       - name: Upload security gate reports
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: security-gate-reports
#           path: |
#             reports/
#             ${{ runner.temp }}/opa-deny.json

#       - name: Write security gate summary
#         if: always()
#         run: |
#           echo "## Security Gate" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
#           echo "|---|---|" >> $GITHUB_STEP_SUMMARY
#           echo "| Checkov CIS AWS | ${{ job.status }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| OPA Policy      | ${{ job.status }} |" >> $GITHUB_STEP_SUMMARY

#   # =======================================================
#   # JOB 3: Deploy to dev (auto on push to main, or manual)
#   # =======================================================
#   deploy-dev:
#     name: Deploy - dev
#     runs-on: ubuntu-latest
#     needs: security-gate
#     if: |
#       (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
#       (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'dev' && github.event.inputs.action == 'apply')
#     environment:
#       name: development
#       url: ${{ steps.outputs.outputs.alb_url }}
#     defaults:
#       run:
#         shell: bash
#         working-directory: ./environments/dev
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v2
#         with:
#           terraform_version: ${{ env.TERRAFORM_VERSION }}

#       - name: Terraform init
#         run: terraform init -backend-config="bucket=${{ env.BUCKET_TF_STATE }}"

#       - name: Terraform plan
#         id: plan
#         run: |
#           terraform plan \
#             -var-file=terraform.tfvars \
#             -out=tfplan \
#             -no-color \
#             -input=false \
#             2>&1 | tee ${{ runner.temp }}/plan-dev.txt

#       - name: Plan status check
#         if: steps.plan.outcome == 'failure'
#         run: exit 1

#       - name: Terraform apply
#         id: apply
#         run: |
#           terraform apply \
#             -auto-approve \
#             -no-color \
#             -input=false \
#             tfplan \
#             2>&1 | tee ${{ runner.temp }}/apply-dev.txt

#       - name: Capture Terraform outputs
#         id: outputs
#         run: |
#           ALB=$(terraform output -raw alb_dns_name 2>/dev/null || echo "")
#           echo "alb_url=http://${ALB}" >> $GITHUB_OUTPUT
#           terraform output -json > ${{ runner.temp }}/outputs-dev.json

#       - name: Upload apply output
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: deploy-dev-output
#           path: |
#             ${{ runner.temp }}/plan-dev.txt
#             ${{ runner.temp }}/apply-dev.txt
#             ${{ runner.temp }}/outputs-dev.json

#       - name: Write deploy summary
#         if: always()
#         run: |
#           echo "## Deploy - dev" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
#           echo "|---|---|" >> $GITHUB_STEP_SUMMARY
#           echo "| Environment | dev |" >> $GITHUB_STEP_SUMMARY
#           echo "| Status      | ${{ steps.apply.outcome }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| ALB URL     | ${{ steps.outputs.outputs.alb_url }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| Triggered by | @${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| Commit      | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY

#   # =======================================================
#   # JOB 4: Deploy to prod (manual only, requires GitHub
#   #         Environment approval - configure reviewers at:
#   #         Settings > Environments > production)
#   # =======================================================
#   deploy-prod:
#     name: Deploy - prod
#     runs-on: ubuntu-latest
#     needs: security-gate
#     if: |
#       github.event_name == 'workflow_dispatch' &&
#       github.event.inputs.environment == 'prod' &&
#       github.event.inputs.action == 'apply'
#     environment:
#       name: production
#       url: ${{ steps.outputs.outputs.alb_url }}
#     defaults:
#       run:
#         shell: bash
#         working-directory: ./environments/prod
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID_PROD }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_PROD }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v2
#         with:
#           terraform_version: ${{ env.TERRAFORM_VERSION }}

#       - name: Terraform init
#         run: terraform init -backend-config="bucket=${{ env.BUCKET_TF_STATE }}"

#       - name: Terraform plan
#         id: plan
#         run: |
#           terraform plan \
#             -var-file=terraform.tfvars \
#             -out=tfplan \
#             -no-color \
#             -input=false \
#             2>&1 | tee ${{ runner.temp }}/plan-prod.txt

#       - name: Show plan in summary
#         run: |
#           echo "## Terraform Plan - prod" >> $GITHUB_STEP_SUMMARY
#           echo '```hcl' >> $GITHUB_STEP_SUMMARY
#           head -200 ${{ runner.temp }}/plan-prod.txt >> $GITHUB_STEP_SUMMARY || true
#           echo '```' >> $GITHUB_STEP_SUMMARY

#       - name: Plan status check
#         if: steps.plan.outcome == 'failure'
#         run: exit 1

#       - name: Terraform apply
#         id: apply
#         run: |
#           terraform apply \
#             -auto-approve \
#             -no-color \
#             -input=false \
#             tfplan \
#             2>&1 | tee ${{ runner.temp }}/apply-prod.txt

#       - name: Capture Terraform outputs
#         id: outputs
#         run: |
#           ALB=$(terraform output -raw alb_dns_name 2>/dev/null || echo "")
#           echo "alb_url=https://${ALB}" >> $GITHUB_OUTPUT
#           terraform output -json > ${{ runner.temp }}/outputs-prod.json

#       - name: Upload apply output
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: deploy-prod-output
#           path: |
#             ${{ runner.temp }}/plan-prod.txt
#             ${{ runner.temp }}/apply-prod.txt
#             ${{ runner.temp }}/outputs-prod.json

#       - name: Write deploy summary
#         if: always()
#         run: |
#           echo "## Deploy - prod" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
#           echo "|---|---|" >> $GITHUB_STEP_SUMMARY
#           echo "| Environment | production |" >> $GITHUB_STEP_SUMMARY
#           echo "| Status      | ${{ steps.apply.outcome }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| ALB URL     | ${{ steps.outputs.outputs.alb_url }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| Approved by | @${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| Commit      | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
#           echo "| Run ID      | ${{ github.run_id }} |" >> $GITHUB_STEP_SUMMARY

#   # =======================================================
#   # JOB 5: Destroy (workflow_dispatch only, requires confirm)
#   # =======================================================
#   destroy:
#     name: Destroy infra
#     runs-on: ubuntu-latest
#     needs: security-gate
#     if: |
#       github.event_name == 'workflow_dispatch' &&
#       github.event.inputs.action == 'destroy' &&
#       github.event.inputs.destroy_confirm == 'DESTROY'
#     defaults:
#       run:
#         shell: bash
#         working-directory: ./environments/${{ github.event.inputs.environment }}
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v2
#         with:
#           terraform_version: ${{ env.TERRAFORM_VERSION }}

#       - name: Terraform init
#         run: terraform init -backend-config="bucket=${{ env.BUCKET_TF_STATE }}"

#       - name: Terraform destroy plan
#         id: destroy_plan
#         run: |
#           terraform plan \
#             -destroy \
#             -var-file=terraform.tfvars \
#             -out=tfplan_destroy \
#             -no-color \
#             -input=false

#       - name: Destroy plan status check
#         if: steps.destroy_plan.outcome == 'failure'
#         run: exit 1

#       - name: Terraform destroy
#         run: |
#           terraform destroy \
#             -auto-approve \
#             -var-file=terraform.tfvars \
#             -no-color \
#             -input=false
#         env:
#           TF_INPUT: false

#       - name: Write destroy summary
#         if: always()
#         run: |
#           echo "## Destroy - ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
#           echo "|---|---|" >> $GITHUB_STEP_SUMMARY
#           echo "| Environment | ${{ github.event.inputs.environment }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| Status      | ${{ job.status }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| Destroyed by | @${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| Commit      | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY

#   # =======================================================
#   # JOB 6: Notify via Slack on completion
#   # =======================================================
#   notify-slack:
#     name: Notify Slack
#     runs-on: ubuntu-latest
#     needs: [deploy-dev, deploy-prod, destroy]
#     if: always()
#     steps:
#       - name: Determine deploy status
#         id: status
#         run: |
#           DEV="${{ needs.deploy-dev.result }}"
#           PROD="${{ needs.deploy-prod.result }}"
#           DESTROY="${{ needs.destroy.result }}"

#           if [ "$DEV" = "success" ] || [ "$PROD" = "success" ] || [ "$DESTROY" = "success" ]; then
#             echo "status=success" >> $GITHUB_OUTPUT
#           elif [ "$DEV" = "failure" ] || [ "$PROD" = "failure" ] || [ "$DESTROY" = "failure" ]; then
#             echo "status=failure" >> $GITHUB_OUTPUT
#           else
#             echo "status=skipped" >> $GITHUB_OUTPUT
#           fi

#       - name: Notify Slack channel
#         if: steps.status.outputs.status != 'skipped'
#         uses: rtCamp/action-slack-notify@v2
#         env:
#           SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
#           SLACK_COLOR: ${{ steps.status.outputs.status == 'success' && 'good' || 'danger' }}
#           SLACK_TITLE: "Terraform Deploy Status"
#           SLACK_MESSAGE: |
#             Environment : ${{ github.event.inputs.environment || 'dev' }}
#             Action      : ${{ github.event.inputs.action || 'apply' }}
#             Branch      : ${{ github.ref_name }}
#             Triggered by: ${{ github.actor }}
#             Status      : ${{ steps.status.outputs.status }}
#             Run         : ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
